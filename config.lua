Config = {}

-- 📌 Position possible :
-- "top-right"    → En haut à droite
-- "bottom-right" → En bas à droite
-- "top-center"   → En haut centré
-- "top-left"     → En haut à gauche
Config.Position = "bottom-right"

-- 📌 Taille (en % de la largeur de l'écran)
Config.WidthVW = 10 -- par défaut = 10vw (10% largeur écran)

-- 📌 Taille min / max (sécurité multi-résolutions)
Config.MinWidth = 80   -- px minimum
Config.MaxWidth = 200  -- px maximum

-- 📌 Opacité (0.0 = invisible, 1.0 = opaque)
Config.Opacity = 0.9
